name: enricher e2e (docker compose, mock-foundry + mock-gemini)

testcases:
  - name: docker compose smoke (mock-foundry + mock-gemini)
    steps:
      - type: exec
        script: bash ../scripts/e2e_docker_compose.sh
        assertions:
          - result.code ShouldEqual 0
