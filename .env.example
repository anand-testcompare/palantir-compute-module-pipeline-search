# Local development environment variables.
# This repo is public. Never commit real secrets to git.

# Gemini (required for real enrichment)
GEMINI_API_KEY=
# Example values: gemini-2.5-flash, gemini-2.5-pro (pick based on your account/quota)
GEMINI_MODEL=

# Optional: override Gemini API base URL (useful for local hermetic mock servers)
GEMINI_BASE_URL=

# Optional: include sources/queries in output CSV (debug/audit)
GEMINI_CAPTURE_AUDIT=

# Foundry (required only when running in Foundry/pipeline mode)
FOUNDRY_URL=

# Optional tuning knobs
WORKERS=
MAX_RETRIES=
REQUEST_TIMEOUT=
FAIL_FAST=

# Optional: global request rate limit (requests per second). 0 disables.
RATE_LIMIT_RPS=
